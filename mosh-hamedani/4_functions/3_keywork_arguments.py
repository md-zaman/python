def increment(number, by):
    return number + by


result = increment(2, 1)
print(result)

#2nd way:

print(increment(2, 1)) #First evalutes the braket inside, stores the value in a variable and then returns to print

# Making code more readable:
print(increment(number=2, by=1)) #adding number=2 makes the code more readable


